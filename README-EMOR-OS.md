# EMOR OS – Founders Console & Lead Intelligence

EMOR OS is the internal operating system for EMOR AI founders.

It centralizes:
- lead capture
- qualification
- notes management
- follow-up workflow
- lightweight analytics

This OS exists to **close clients faster** with no dropped leads.

Docs live in `/docs`. Database migrations live in `/db/migrations`.
